* add double left click to zoom main image + shift = unzoom, (x0.1->x10)
* handle detail image display when main image zooms
* mode to lock detail image: key press or mouse (right click)
* display image source x,y display above detail image + locked status
